export default defineEventHandler(async (event) => {
  //const name = getRouterParam(event, "userId");
  //const signUp = getRouterParam(event, "signUp");
  //We can also GET user from custom endpoint to match if is a signed up user
  //Step implemented in the POST api function
});
